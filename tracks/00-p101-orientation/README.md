# p101 orientation

Track number: `00`

Wrapper count: `0` direct assignments

This is the hand-authored pre-track. It does not own any wrappers in the
generated function graph; instead, it teaches how to read every later track.

Start here before Track 01.

```sh
./tracks/00-p101-orientation/run.sh
```

The runner writes its report to `/tmp/p101-track-orientation` by default.

Read the curated lesson guide: [TRACK.md](./TRACK.md).

